sudo: false
language: python
python:
  - "3.4"

cache: false

install:
  - pip install -r requirements.txt

script:
  - django-admin test

notifications:
  email: false

branches:
  only:
    - master
